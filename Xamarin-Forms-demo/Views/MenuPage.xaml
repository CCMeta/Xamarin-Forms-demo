<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Xamarin_Forms_demo.Views.MenuPage"
             Visual="Material" 
             Title="MenuPage">
    <StackLayout>
        <Image HeightRequest="200" BackgroundColor="DeepPink" x:Name="ImageAvatar" Source="{Binding avatar}" VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand"/>
        <Label Text="{Binding username}" />
        <Label Text="{Binding avatar}" />
        <ListView x:Name="ListViewMenu" x:FieldModifier="public">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Grid Padding="10">
                            <Label FontSize="Medium">
                                <Label.FormattedText>
                                    <FormattedString>
                                        <Span Text="&#xf6ad;" FontFamily="FontAwesome5Solid"/>
                                        <Span Text="   "/>
                                        <Span Text="{Binding Title}"/>
                                    </FormattedString>
                                </Label.FormattedText>
                            </Label>
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>